ext {
	next = true;
}

task innerBuildCommon2 (dependsOn: 'gradleCommon2:build'){ //wygl¹da, ¿e 'gradleCommon2:build' jest rownowa¿ne ':gradleCommon2:build'
	doFirst{ println('-> Rozpoczecie build Common') }
	doLast{ println('-> Zakonczenie build Common') }
}

task innerBuildUser (dependsOn: 'GraddleCommonUser:build'){
	doFirst{ println('-> Rozpoczecie build App') }
	doLast{ println('-> Zakonczenie build App') }
}

task buildWholeProject {
	doFirst{ println('-> Rozpoczecie build calego projektu') }
}

innerBuildCommon2.finalizedBy innerBuildUser;
buildWholeProject.finalizedBy innerBuildCommon2


